spring:
  application:
    name: ai-code-backend

server:
  port: 8888
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.jackiy.aicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
